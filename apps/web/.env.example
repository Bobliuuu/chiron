# Chiron WEB frontend environment. Copy to apps/web/.env.local.
#
# The web app is a thin client — it holds NO secrets. It only needs to know
# where the standalone backend lives. In production, point this at the backend's
# custom domain behind Cloudflare (e.g. https://api.chiron.example).
NEXT_PUBLIC_API_URL=http://localhost:8787
