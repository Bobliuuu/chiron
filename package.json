{
  "name": "chiron",
  "private": true,
  "version": "0.1.0",
  "description": "AI-first community event assistant — a channel-aware agent backend with multiple frontends.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @chiron/server",
    "dev:web": "npm run dev --workspace @chiron/web",
    "dev:server": "npm run dev --workspace @chiron/server",
    "dev:whatsapp": "npm run dev --workspace @chiron/whatsapp",
    "build": "npm run build --workspace @chiron/server && npm run build --workspace @chiron/web && npm run build --workspace @chiron/whatsapp",
    "start:server": "npm run start --workspace @chiron/server",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "npm run lint --workspace @chiron/web"
  },
  "dependencies": {
    "resend": "^6.17.2"
  }
}
